# Lab 15 — Git Workflow + GitHub Actions CI/CD

This is a Java Maven project for practicing Git workflow, GitHub Actions, CI/CD, pull requests, matrix build, branch protection, and JaCoCo coverage gate.

## Build

Run this command:

mvn -B clean verify
